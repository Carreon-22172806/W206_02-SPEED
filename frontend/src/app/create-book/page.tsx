'use client'

import CreateBookComponent from "@/components/Book/CreateBook";

export default function CreateBook() {
    return (
        <main>
            <CreateBookComponent />
        </main>
    );
}